# Deliberately narrow: `only_rules`, not `disabled_rules`. SwiftLint's default
# rule set covers two kinds of territory this repo doesn't want it opining on:
#
#   - Layout (colon/comma/opening_brace/line_length/...): swift-format already
#     owns this; running both fights, since they can disagree on the same line.
#   - Code-quality/complexity (identifier_name, cyclomatic_complexity,
#     function_body_length, large_tuple, ...): a separate concern from code
#     style, and one that overlaps the ecosystem's own code-structure policy
#     (a repo that needs a structural pass runs one as its own scoped
#     initiative, not as a side effect of a style-lint gate). identifier_name's
#     default 3-char minimum in particular is not a fit for this codebase's
#     short, conventional locals in dense byte-parsing/geometry code (p, t, a,
#     b, c, m, x, y, z, i, j, n): 48 hits on first run with the full default
#     rule set, 37 of them identifier_name, none of them real problems.
#
# What's left is the one rule that catches something swift-format has no
# equivalent for: a doc comment not attached to any declaration.
#
# See docs/code-style-policy-proposal-2026-08.md in the ecosystem repo for
# the full rationale.

only_rules:
  - orphaned_doc_comment

excluded:
  - .build
  - Tests
